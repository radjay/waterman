import * as React from 'react';

/**
 * MobileMenu — from waterman@1.0.0.
 */
export interface MobileMenuProps {
isOpen: boolean; onOpenChange: (...args: any[]) => void;
}

export declare const MobileMenu: React.ComponentType<MobileMenuProps>;
