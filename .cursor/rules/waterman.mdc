---
alwaysApply: true
---

- Always consult planning/architecture.md when making changes or implementing features in this repo. Update this file whenever needed to accurately reflect the state of the repo.
- When running the dev server (`npm run dev`), always use `required_permissions: ['all']` to bypass sandbox restrictions that prevent binding to network ports.
- When implementing a feature, change or bug, you should always test in the browser tool if its working correctly before concluding it's working!
