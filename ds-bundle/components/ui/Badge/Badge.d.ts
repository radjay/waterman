import * as React from 'react';

/**
 * Badge — from waterman@1.0.0.
 */
export interface BadgeProps {
children?: React.ReactNode; variant?: "epic" | "default"; className?: string;
}

export declare const Badge: React.ComponentType<BadgeProps>;
