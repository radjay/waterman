import * as React from 'react';

/**
 * SessionCard — from waterman@1.0.0.
 */
export interface SessionCardProps {
entry: unknown;
}

export declare const SessionCard: React.ComponentType<SessionCardProps>;
