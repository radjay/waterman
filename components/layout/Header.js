/**
 * Header — retired.
 *
 * The legacy masthead ("The Waterman Report" + date) belonged to the
 * newsprint IA. Primary chrome is now TopNav (desktop) and BottomNav
 * (mobile). Pages still import this component; keep the export so those
 * imports do not break, but render nothing.
 *
 * Prefer PageHeader for in-page titles on Current screens.
 */
export function Header() {
  return null;
}
