import * as React from 'react';

/**
 * UserMenu — from waterman@1.0.0.
 */
export interface UserMenuProps {
  [key: string]: unknown;
}

export declare const UserMenu: React.ComponentType<UserMenuProps>;
