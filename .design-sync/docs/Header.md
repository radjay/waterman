---
category: Current · Layout
---

**Retired.** The legacy masthead ("The Waterman Report" + date) no longer
renders. Primary chrome is `TopNav` (desktop) and `BottomNav` (mobile). The
export remains so older page imports do not break. Prefer `PageHeader` for
in-page titles on Current screens.
