import * as React from 'react';

/**
 * ConvexProvider — from waterman@1.0.0.
 */
export interface ConvexProviderProps {
children?: React.ReactNode;
}

export declare const ConvexProvider: React.ComponentType<ConvexProviderProps>;
