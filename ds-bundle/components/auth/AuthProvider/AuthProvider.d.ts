import * as React from 'react';

/**
 * AuthProvider — from waterman@1.0.0.
 */
export interface AuthProviderProps {
children?: React.ReactNode;
}

export declare const AuthProvider: React.ComponentType<AuthProviderProps>;
