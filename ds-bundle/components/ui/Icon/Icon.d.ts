import * as React from 'react';

/**
 * Icon — from waterman@1.0.0.
 */
export interface IconProps {
children?: React.ReactNode; className?: string;
}

export declare const Icon: React.ComponentType<IconProps>;
